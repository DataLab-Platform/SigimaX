# Optional: Set PYTHONPATH to an explicit interpreter path (e.g. WinPython).
# If not set, run_with_env.py will auto-detect a .venv* directory.
PYTHONPATH=.

# Optional: Set VENV_DIR to select a specific virtual environment directory (e.g. .venv39).
# If not set, run_with_env.py will auto-detect a .venv* directory.
VENV_DIR=

# Optional: Set LANG to a specific language code (e.g. fr).
LANG=